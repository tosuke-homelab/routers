---
all:
  hosts:
    nono:
      ansible_host: nono
      ansible_user: tosuke
    erlite:
      ansible_host: erlite
      ansible_user: admin
    border:
      ansible_host: border
      ansible_user: me
  children:
    # OS
    vyos:
      hosts:
        nono:
        border:
    edgeos:
      hosts:
        erlite:
    # Location
    home:
      hosts:
        nono:
        erlite:
    home_primary:
      hosts:
        nono:
    home_secondsry:
      hosts:
        erlite:
    linode:
      hosts:
        border:
